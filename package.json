{
  "scripts": {
    "heroku-prebuild": "cd server && npm i",
    "heroku-postbuild": "cd client && npm i && npm i --only=dev --no-shrinkwrap && npm run build",
    "start": "cd server && node server.js"
  },
  "devDependencies": {
    "jest": "^28.1.3"
  },
  "dependencies": {
    "maildev": "^2.0.5"
  }
}
